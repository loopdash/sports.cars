# Lighthouse baseline — 2026-09-01

Run against the WP Engine staging install (`sportscars1`) with the staging gate
temporarily lifted for the run. Desktop, Lighthouse 11. Scores are 0–100.

| Page | Performance | Accessibility | Best Practices | SEO |
|---|---|---|---|---|
| home | 54 | 92 | 81 | 91 |
| search | 57 | 88 | 56 | 91 |
| listing | 57 | 95 | 100 | 82 |

## Target: 85+ on all three (per the brief). Performance is the gap (~55).

### Top performance opportunities

**home**
- ~7970 ms — Defer offscreen images
- ~4830 ms — Properly size images
- ~4590 ms — Serve images in next-gen formats
- ~3261 ms — Eliminate render-blocking resources
- ~1168 ms — Avoid multiple page redirects
- ~480 ms — Efficiently encode images

**search**
- ~4950 ms — Properly size images
- ~2400 ms — Serve images in next-gen formats
- ~1183 ms — Eliminate render-blocking resources
- ~878 ms — Avoid multiple page redirects
- ~450 ms — Reduce unused JavaScript
- ~435 ms — Initial server response time was short

**listing**
- ~33830 ms — Properly size images
- ~19710 ms — Serve images in next-gen formats
- ~6900 ms — Defer offscreen images
- ~1059 ms — Eliminate render-blocking resources
- ~851 ms — Avoid multiple page redirects
- ~750 ms — Reduce unused JavaScript

### Likely quick wins
- `font-display: swap` + preconnect for the Saira Google Font (render-blocking).
- Serve/resize hero + Wikipedia images (they're full-res); add width/height + `loading="lazy"`.
- Defer non-critical JS; the search page's Best-Practices (56) is dragged down by console errors (the staging gate 501 + GA collect abort) that clear in production.

_Re-run after each optimization; commit updated scores here._
## Run 2 — after font-async + below-fold lazy-load

| Page | Performance | Accessibility | Best Practices | SEO |
|---|---|---|---|---|
| home | 51 | 92 | 81 | 91 |
| search | 60 | 88 | 96 | 91 |
| listing | 57 | 95 | 100 | 82 |

- Made the Google Font non-render-blocking; lazy-loaded below-fold images.
- **Best Practices 56→96 on search**: the earlier 56 was the staging gate's
  501 console errors — those disappear in production (gate off).
- **Performance is still ~55, not 85.** `loading="lazy"` can't defer the hero
  images (all in-viewport). Reaching 85 needs a dedicated pass:
  JS-defer the 7 non-active hero slides, resize + serve next-gen (AVIF/WebP)
  images at display size, and trim/defer JS. Tracked as remaining work.
